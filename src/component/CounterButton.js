import React from 'react';

const CounterButton = () => {
    return (
        <div>
            <button >증가 (+)</button>
            <button >감소 (-)</button>
        </div>
    );
};

export default CounterButton